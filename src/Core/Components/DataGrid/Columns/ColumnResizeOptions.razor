@namespace Microsoft.FluentUI.AspNetCore.Components
@typeparam TGridItem

<FluentStack Class="resize-options" VerticalAlignment="VerticalAlignment.Center" HorizontalGap="4">
    @if (ResizeType == DataGridResizeType.Discrete)
    {
        <FluentLabel>@Label</FluentLabel>

        <FluentSpacer />
        <GButton OnClick="@HandleShrinkAsync" IconStart="@(new CoreIcons.Regular.Size20.Subtract())" />
        <GButton OnClick="@HandleGrowAsync" IconStart="@(new CoreIcons.Regular.Size20.Add())" />
        <GButton OnClick="@HandleResetAsync" IconStart="@(new CoreIcons.Regular.Size20.ArrowReset())" />
    }
    else
    {
        <FluentStack Orientation="Orientation.Horizontal" HorizontalAlignment="HorizontalAlignment.End" VerticalAlignment="VerticalAlignment.Bottom">
            <div style="display: flex; flex-direction: column; ">
                <FluentTextField Id="width"
                                 @bind-Value="@_width"
                                 Style="width: 100%;"
                                 TextFieldType="TextFieldType.Text"
                                 InputMode="InputMode.Numeric"
                                 Immediate="true"
                                 ImmediateDelay="200"
                                 Label="@Label"
                                 AutoComplete="off"
                                 @onkeydown="@HandleColumnWidthKeyDownAsync" />
            </div>
            <GButton Style="height: 30px;" OnClick="@HandleColumnWidthAsync" Appearance="@Appearance.Accent">
                <FluentIcon Value="@(new CoreIcons.Regular.Size20.Checkmark())" Color="@Color.Fill" />
            </GButton>
            <GButton Style="height: 30px;" OnClick="@HandleResetAsync" IconStart="@(new CoreIcons.Regular.Size20.ArrowReset())" />
        </FluentStack>
    }
</FluentStack>

