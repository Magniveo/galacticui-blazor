<h4>Horizontal</h4>
<FluentStack>
    <GCheckbox @bind-Value="@value1" Label="Apples" />
    <GCheckbox @bind-Value="@value2" Disabled="true" Label="Bananas (disabled)" />
    <GCheckbox @bind-Value="@value3" Label="Oranges" />
</FluentStack>

<br />
<br />

<h4>Vertical</h4>
<FluentStack Orientation="Orientation.Vertical">
    <GCheckbox @bind-Value="@value1">Apples</GCheckbox>
    <GCheckbox @bind-Value="@value2" Disabled="true">Bananas (disabled)</GCheckbox>
    <GCheckbox @bind-Value="@value3">Oranges</GCheckbox>
</FluentStack>

@code {
    bool value1 = true;
    bool value2 = true;
    bool value3;
}