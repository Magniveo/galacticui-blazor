
<GButton Appearance="Appearance.Accent" @onclick="() => visible = !visible">Show Overlay</GButton>

<FluentOverlay @bind-Visible=@visible OnClose="HandleOnClose">
    <FluentProgressRing />
</FluentOverlay>

@code {
    bool visible = false;

    protected void HandleOnClose()
    {
        DemoLogger.WriteLine("Transparent overlay closed");
    }
}
